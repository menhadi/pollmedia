# Historical election JSON database package — 24 September 2026

The local election and by-election archives contain 5,867 JSON files: 999 under `election-archive` and 4,868 under `election-by-elections`. They include collected-source manifests, extracted historical PC/AC candidate records, by-election raw tables, summaries and structured results. Original PDFs remain in private R2; source HTML and Excel files are preserved both locally and in private R2 and are not included in this JSON package.

`pilot/preserve_archive_json.py` split the exact JSON bytes into eight deterministic buckets per category. All **16** ZIPs passed SHA-256 and `archive:import-json --check` validation, covering all 5,867 files. Total compressed size is about **49.2 MiB**. `exports/pollmedia-archive-json-20260924.sha256` uses Unix line endings so `sha256sum -c` works on Linux. The exporter refuses to overwrite an existing package.

The 16 ZIPs and checksum list are staged at `/home/pollmedia/tmp/archive-json-20260924/`. Server-side `sha256sum -c pollmedia-archive-json-20260924.sha256` passed for all 16 ZIPs. On 24 September, the user pulled application code `0405c30` and ran the three archive migrations. All 16 packages were then imported sequentially with `php8.4 application/artisan archive:import-json PACKAGE --sha256=EXPECTED_HASH`. The command verified each entry's path, bucket, length, SHA-256 and JSON syntax and rejected conflicting replacements. Independent PostgreSQL counts confirm **5,867** `archive_json_files` rows: 999 election archive and 4,868 by-election. About 19.2 GiB remained free. `ArchiveFiles` can read these records when local JSON is absent.

This package does **not** populate `election_contests` or mark results accepted. It preserves raw and extracted archive evidence for the existing historical pages. Those pages still verify their source PDFs, so their live use also requires a tested R2 profile and linked archive PDF receipts. This package alone does not establish complete national election coverage; inaccessible official sources, unmapped layouts and unverified OCR remain recorded gaps.

The separate `exports/archived-r2-receipts.jsonl` has 2,090 read-back verified PDF receipts: 1,687 election archive, 402 by-election and one census. Its SHA-256 is `53d47f290d75f32cef0aaaa86ec5a265a2e3e06fb3a8f47f2a5a93805b54d634`. All receipts match the local archive inventory on path, SHA-256, bytes and official source URL. The 252 election PDFs recovered from preserved official category and download-page HTML carry their recovery evidence in the receipt. `archive:link-r2` validates that checksum, a tested private R2 profile, each path/object key/hash/size, and conflicts before registering PDF inventory and official source evidence in one database transaction. The `archive_pdf_sources` table was created by migration. The archived receipt file is staged at `/home/pollmedia/tmp/archived-r2-receipts.jsonl`; its server-side SHA-256 and size match the local file. Linking now waits only for a tested private R2 profile; it does not upload files or change accepted election results.

The separate source-original phase has **2,890** read-back verified HTML/XLS/XLSX/ZIP receipts, covering **192,430,345** local bytes. Each receipt matches the local inventory on path, SHA-256, bytes, category and official source URL. Its `url_scope` states whether the URL names a download (973), a source page (1,908), or only the official collection page (nine). The nine collection URLs are not presented as exact download links. Files are under the private R2 prefix `pollmedia/archive-source-originals/`; local copies are retained. The checksum-verified receipt file is staged at `/home/pollmedia/tmp/archive-originals-r2-receipts.jsonl` with SHA-256 `013a40d3ca71f1db9afdbe32a89580e7394d89f883b5836da8f523faa8df00dd` (1,726,327 bytes). After the private R2 profile test, `archive:link-originals --receipts=/home/pollmedia/tmp/archive-originals-r2-receipts.jsonl --sha256=013a40d3ca71f1db9afdbe32a89580e7394d89f883b5836da8f523faa8df00dd --profile=PROFILE_ID` registers those files and source URLs in `archive_original_files`. `ArchiveFiles` can then stream or stage an original when no local copy exists. No live source-original linking has run yet.
