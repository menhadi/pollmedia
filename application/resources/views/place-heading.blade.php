<div class="place-page-heading"><nav class="place-breadcrumbs" aria-label="Breadcrumb"><ol>@foreach($placeTrail as $crumb)<li>@if($loop->last)<span aria-current="page">{{ $crumb['label'] }}</span>@elseif($crumb['url'])<a href="{{ $crumb['url'] }}">{{ $crumb['label'] }}</a>@else<span>{{ $crumb['label'] }}</span>@endif</li>@endforeach</ol></nav><h1>{{ $headingName }}</h1><p class="place-type">{{ $headingType }}</p></div>
