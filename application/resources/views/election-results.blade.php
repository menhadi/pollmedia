<section id="elections" aria-labelledby="election-heading">
    <div class="kicker">Politics & elections</div><h2 id="election-heading">Election results & voting trends</h2>
    @foreach($withheldElections as $withheld)<p class="notice"><strong>{{ $withheld->year }} results withheld.</strong> {{ $withheld->error }} <a href="{{ $withheld->source_url }}" target="_blank" rel="noreferrer">Official report edition ↗</a></p>@endforeach
    @if($elections->isEmpty())
        <div class="card"><p>{{ $type === 'district' ? 'Election results belong to constituencies, not district totals.' : 'Election results have not been imported for this constituency.' }} Explore the connected Assembly constituencies for their available results.</p><a class="button" href="#related-places">Explore connected constituencies →</a></div>
    @else
        <p class="lead">{{ $type === 'ac' ? 'Assembly election' : 'Lok Sabha election' }} totals from ECI statistical reports. Parties shown here are the parties at the time of each election.</p>
        <form method="get" action="{{ route('places.show', ['type' => $type, 'slug' => substr($place->slug, strlen($type) + 1)]) }}#elections" style="margin-bottom:20px"><label for="election-year">Election year</label> <select id="election-year" name="year" class="search" style="width:120px">@foreach($elections as $e)<option value="{{ $e->year }}" @selected($e->year===$selectedElection->year)>{{ $e->year }}</option>@endforeach</select> <button class="button" type="submit" style="border:0">Show results</button></form>
        @php($e = $selectedElection)
        <div class="stats"><div class="stat"><small>{{ $e->year }} winner · {{ $e->winner->party_at_election }}</small><h3>{{ $e->winner->candidate_name }}</h3><strong>{{ number_format($e->winner->votes) }}</strong><small>Votes received</small></div><div class="stat"><small>Winning margin</small><strong>{{ number_format($e->margin) }}</strong><small>{{ number_format($e->margin_pp,2) }} percentage points of all votes polled</small></div><div class="stat"><small>Recorded-vote participation</small><strong>{{ number_format($e->turnout,2) }}%</strong><small>{{ number_format($e->counted_votes) }} candidate + NOTA votes / {{ number_format($e->electors) }} electors</small></div></div>
        <p class="small"><a href="{{ $e->url }}" target="_blank" rel="noreferrer">Official ECI report ↗</a> · {{ $e->source_locator }} · Checked {{ substr($e->retrieved_at,0,10) }} @if($e->totals_url) · <a href="{{ $e->totals_url }}" target="_blank" rel="noreferrer">Official totals report ↗</a>@endif</p>
        <div class="grid2"><article class="card"><h3>Vote share · {{ $e->year }}</h3><p class="small">Share of all votes polled, including NOTA and any residual votes. Scale: 0–100%.</p>
            @foreach($e->rows as $row)
                @php($share=100*$row->votes/$e->votes_polled)
                <div style="margin:13px 0"><div style="display:flex;justify-content:space-between;gap:14px;font-size:12px"><span>{{ $row->candidate_name }} · {{ $row->party_at_election }}</span><strong>{{ number_format($share,2) }}%</strong></div><div style="height:9px;background:#edf0e9;border-radius:3px" aria-hidden="true"><div style="height:100%;width:{{ $share }}%;background:{{ $row->is_nota?'#8b8e85':'#176c55' }};border-radius:3px"></div></div></div>
            @endforeach
            <p class="small">All votes polled: {{ number_format($e->votes_polled) }}. Residual {{ number_format($e->unallocated) }} votes are outside candidate + NOTA totals; their classification is not inferred.</p>
        </article><article class="card"><h3>Historical winning margins</h3><p class="small">Winner minus runner-up, in votes. Bar lengths share a common scale.</p>
            @foreach($elections->sortBy('year') as $past)
                <div style="margin:24px 0"><strong>{{ $past->year }} · {{ number_format($past->margin) }} votes</strong><p class="small">{{ $past->winner->candidate_name }} ({{ $past->winner->party_at_election }}) over {{ $past->runner->candidate_name }} ({{ $past->runner->party_at_election }})</p><div style="height:12px;background:#edf0e9" aria-hidden="true"><div style="height:100%;width:{{ $elections->max('margin')>0 ? 100*$past->margin/$elections->max('margin') : 0 }}%;background:#176c55"></div></div><a class="small" href="{{ $past->url }}" target="_blank" rel="noreferrer">{{ $past->year }} source ↗</a></div>
            @endforeach
            <p class="small">Coverage: {{ $elections->pluck('year')->sort()->implode(', ') }} only. No booth, village or demographic voting patterns are inferred. Boundary compatibility must be checked before interpreting changes as a swing.</p>
        </article></div>
        <div class="card" style="margin-top:20px"><h3>Full candidate results · {{ $e->year }}</h3><div class="table"><table><caption class="small">ECI report {{ $e->source_locator }}; share = candidate votes ÷ all votes polled.</caption><thead><tr><th>Candidate / option</th><th>Party at election</th><th>General votes</th><th>Postal votes</th><th>Total votes</th><th>Share</th></tr></thead><tbody>@foreach($e->rows as $row)<tr><td>{{ $row->candidate_name }}</td><td>{{ $row->party_at_election }}</td><td>{{ number_format($row->general_votes) }}</td><td>{{ number_format($row->postal_votes) }}</td><td>{{ number_format($row->votes) }}</td><td>{{ number_format(100*$row->votes/$e->votes_polled,2) }}%</td></tr>@endforeach</tbody></table></div></div>
        <p class="notice">ECI statistical reports support research and may differ from election-night figures. Returning Officers’ statutory forms remain the final authority. This local pilot retains the source edition and calculation basis.</p>
    @endif
    @if(in_array($place->slug, ['ac-pilibhit', 'pc-pilibhit', 'district-pilibhit']))<article class="card" style="margin-top:20px"><h3>Special Intensive Revision</h3><p>Explore the available December 2025 enumeration sample: 1,784 listed records across 20 parts of Pilibhit AC. Partial coverage; these are not final voter totals.</p><a class="button" href="/india/sir">Open SIR explorer →</a></article>@endif
</section>
