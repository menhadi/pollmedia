<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1">
@include('seo-metadata', ['breadcrumbs'=>['India'=>route('home'),$title=>route('states.show',['state'=>$state])], 'canonical'=>route('states.show',['state'=>$state]), 'seoTitle'=>$title.' election results & historical trends · Pollmedia', 'seoDescription'=>'Explore available turnout, party vote shares and winning margins with constituency results and official sources.'])
<link rel="stylesheet" href="/css/election-dashboard.css"></head><body class="election-ui">
<header class="topbar"><a class="brand" href="{{ route('home') }}">pollmedia.</a><nav><a href="{{ route('elections.history') }}">Lok Sabha</a><a href="{{ route('elections.assembly') }}">Assembly</a><a href="{{ route('elections.by-election-results') }}">By-elections</a><a href="{{ route('sources.index') }}">Sources</a></nav></header>
<div class="dashboard-shell"><aside class="dashboard-sidebar"><details><summary>Explore states & linked places</summary><nav aria-label="Related places"><p class="sidebar-label">{{ $title }}</p><a href="#turnout">Turnout over time</a><a href="#margins">Winning margins</a><a href="#parties">Party vote shares</a><a href="#constituencies">Constituency results</a><a href="#politics">Linked profiles</a><a href="{{ route('elections.constituencies', ['state'=>$title,'kind'=>$kind]) }}">Find a constituency →</a><p class="sidebar-label">States & Union Territories</p>@foreach($states as $item)<a href="{{ route('states.show',['state'=>$item['slug'], 'election'=>$kind]) }}" @if($state===$item['slug']) aria-current="page" @endif>{{ $item['name'] }}</a>@endforeach</nav></details></aside>
<main class="dashboard-main"><nav class="breadcrumbs" aria-label="Breadcrumb"><a href="{{ route('home') }}">India</a><span>/</span><span>{{ $title }}</span></nav><p class="eyebrow">{{ $stateSummary['kind'] }} / Election analysis</p><h1>{{ $title }},<br>election by election.</h1><p>Follow the vote over time, then open a constituency for its candidates, figures and sources.</p>
<nav class="tabs" aria-label="Election type"><a href="{{ route('states.show',['state'=>$state,'election'=>'ac']) }}" @if($kind==='ac') aria-current="page" @endif>Assembly</a><a href="{{ route('states.show',['state'=>$state,'election'=>'pc']) }}" @if($kind==='pc') aria-current="page" @endif>Lok Sabha</a><a href="{{ route('elections.by-election-results') }}">By-elections ↗</a></nav>
<section class="panel"><form class="filters" method="get"><input type="hidden" name="election" value="{{ $kind }}"><div><label for="edition">Election year / source edition</label><select id="edition" name="edition" @disabled(!$history)>@foreach($history as $row)<option value="{{ $row['id'] }}" @selected($edition===$row['id'])>{{ $row['label'] }}</option>@endforeach</select></div><p class="small">Changing the edition updates the figures and links below. Separate reports from the same year stay separate.</p><button @disabled(!$history)>Show election</button></form></section>
@if($election)
<div class="metrics"><div class="metric"><span>Votes polled · covered contests</span><strong>{{ $election['polled'] === null ? '—' : number_format($election['polled']) }}</strong><span>{{ $election['turnout_count'] }} of {{ $election['tables'] }} tables</span></div><div class="metric"><span>Turnout</span><strong>{{ $election['turnout'] === null ? '—' : number_format($election['turnout'],1).'%' }}</strong><span>Of electors in those same contests</span></div><div class="metric"><span>Mean winning margin</span><strong>{{ $election['margin'] === null ? '—' : number_format($election['margin']) }}</strong><span>Votes · {{ $election['margin_count'] }} contests</span></div><div class="metric"><span>Available constituency tables</span><strong>{{ number_format($election['tables']) }}</strong><span>{{ $election['year'] }} · {{ strtoupper($kind) }}</span></div></div>
<p class="coverage-note">These are aggregates of the available eligible constituency tables, not a certified statewide total. Coverage can differ by year. Disputed, multi-seat and repeated constituency records remain available in the source tables but are excluded from these charts. Historical boundaries and party names can change.</p>
<section class="panel" id="turnout"><div class="panel-heading"><h2>How turnout changed</h2><span class="small">Votes polled / electors</span></div><p class="small">Bars use a shared 0–100% scale. Amber bars have fewer eligible tables than the available edition. The percentage is calculated from summed votes and electors, not an average of constituency percentages.</p>
@include('election-trend',['metric'=>'turnout','maximum'=>100,'suffix'=>'%','chartLabel'=>'Historical turnout percentages'])
<div class="table-scroll" tabindex="0" role="region" aria-label="Turnout data"><table><caption>Absolute turnout and percentage by source edition</caption><thead><tr><th>Election edition</th><th class="num">Electors</th><th class="num">Votes polled</th><th class="num">Turnout</th><th>Coverage</th></tr></thead><tbody>@foreach($history as $row)<tr><td><a href="{{ route('states.show',['state'=>$state,'election'=>$kind,'edition'=>$row['id']]) }}">{{ $row['label'] }}</a></td><td class="num">{{ $row['electors']===null?'—':number_format($row['electors']) }}</td><td class="num">{{ $row['polled']===null?'—':number_format($row['polled']) }}</td><td class="num">{{ $row['turnout']===null?'—':number_format($row['turnout'],2).'%' }}</td><td>{{ $row['turnout_count'] }} / {{ $row['tables'] }} tables</td></tr>@endforeach</tbody></table></div></section>
<section class="panel" id="margins"><div class="panel-heading"><h2>How close were the contests?</h2><span class="small">Mean winning margin</span></div><p class="small">Winner’s votes minus runner-up’s votes, averaged across eligible contests. Percentage points use the candidate-plus-NOTA vote total of each contest. Bars share a scale from zero to the largest displayed mean.</p>
@include('election-trend',['metric'=>'margin','maximum'=>max(1,collect($history)->max('margin')??0),'suffix'=>'','decimals'=>0,'chartLabel'=>'Mean winning margins in votes'])
<div class="table-scroll" tabindex="0" role="region" aria-label="Winning margin data"><table><caption>Winning margin by source edition</caption><thead><tr><th>Election edition</th><th class="num">Mean margin · votes</th><th class="num">Mean margin · pp</th><th>Contests</th></tr></thead><tbody>@foreach($history as $row)<tr><td>{{ $row['label'] }}</td><td class="num">{{ $row['margin']===null?'—':number_format($row['margin']) }}</td><td class="num">{{ $row['margin_percent']===null?'—':number_format($row['margin_percent'],2) }}</td><td>{{ $row['margin_count'] }}</td></tr>@endforeach</tbody></table></div></section>
<section class="panel" id="parties"><h2>Party vote shares</h2><p class="small">Shares use all recorded candidate votes plus NOTA in eligible tables. Independent candidates are grouped under their reported label. Party names are kept as they appeared at the election.</p><form class="filters" method="get"><input type="hidden" name="election" value="{{ $kind }}"><input type="hidden" name="edition" value="{{ $edition }}"><div><label for="party">Party to compare over time</label><input id="party" name="party" list="party-options" value="{{ $party }}" placeholder="Type a party label"><datalist id="party-options">@foreach($partyOptions as $option)<option value="{{ $option }}"></option>@endforeach</datalist></div><span></span><button>Compare party</button></form>
@php $partyHistory=array_map(function($row) use($party){ $p=collect($row['parties'])->firstWhere('party',$party); return $row+['party_share'=>$p['share']??null]; },$history); @endphp
@include('election-trend',['history'=>$partyHistory,'metric'=>'party_share','maximum'=>100,'suffix'=>'%','decimals'=>1,'chartLabel'=>'Party vote share over time'])
<p class="small">No data means the party is absent from this available aggregate; it does not establish zero votes.</p>
<div class="table-scroll" tabindex="0" role="region" aria-label="Party share history"><table><caption>{{ $party ?? 'Party' }} across available editions</caption><thead><tr><th>Election edition</th><th class="num">Votes</th><th class="num">Share</th><th>Coverage</th></tr></thead><tbody>@foreach($history as $row)@php $p=collect($row['parties'])->firstWhere('party',$party); @endphp<tr><td>{{ $row['label'] }}</td><td class="num">{{ $p?number_format($p['votes']):'—' }}</td><td class="num">{{ $p?number_format($p['share'],2).'%':'—' }}</td><td>{{ $row['party_count'] }} tables</td></tr>@endforeach</tbody></table></div>
<h3>{{ $election['year'] }} · all available parties</h3><div class="table-scroll" tabindex="0" role="region" aria-label="Selected election party votes"><table><caption>{{ $election['label'] }} · {{ $election['party_count'] }} eligible tables</caption><thead><tr><th>Party at election</th><th class="num">Votes</th><th class="num">Vote share</th></tr></thead><tbody>@forelse($election['parties'] as $p)<tr><td>{{ $p['party'] }}</td><td class="num">{{ number_format($p['votes']) }}</td><td class="num">{{ number_format($p['share'],2) }}%</td></tr>@empty<tr><td colspan="3">No eligible candidate totals are available for this edition.</td></tr>@endforelse</tbody></table></div></section>
<section class="panel" id="constituencies"><h2>Go deeper into {{ $election['year'] }}</h2><p>Open the constituency list, choose a seat, then view candidate votes or generate its result report. Original figures and review notes remain on each result.</p><a class="button" href="{{ route($kind==='ac'?'elections.assembly':'elections.history',['edition'=>$edition,'state'=>$election['state']]) }}">Browse {{ $election['tables'] }} constituency tables →</a><p><a href="{{ $election['source_url'] }}">Official source edition ↗</a></p></section>
@else<div class="panel"><h2>Historical analysis is not available for this selection yet</h2><p>Use the source archive to inspect available records. Missing results are not zero turnout or zero votes.</p><a href="{{ route($kind==='ac'?'elections.assembly':'elections.history') }}">Open the result archive →</a></div>@endif
<section class="panel" id="politics"><h2>Linked constituency & district profiles</h2><form class="filters" method="get"><input type="hidden" name="election" value="{{ $kind }}"><div><label for="place">Choose an available place</label><select name="place" id="place"><option value="">All linked profiles</option>@foreach($options as $option)<option value="{{ $option->slug }}" @selected($selected===$option->slug)>{{ $option->name }} {{ strtoupper($option->type) }}</option>@endforeach</select></div><div><label for="type">Page type</label><select name="type" id="type"><option value="">All types</option>@foreach(['pc'=>'PC','ac'=>'AC','district'=>'District'] as $key=>$label)<option value="{{ $key }}" @selected($type===$key)>{{ $label }}</option>@endforeach</select></div><button>Find profile</button></form><p class="small">{{ $places->total() }} available pages. These profiles require verified place relationships; historical archive coverage is broader.</p><div class="place-list">@foreach($places as $place)<a href="{{ route('places.show',['type'=>$place->type,'slug'=>substr($place->slug,strlen($place->type)+1)]) }}">{{ $place->name }} {{ strtoupper($place->type) }} →</a>@endforeach</div>{{ $places->links() }}</section>
<section class="panel"><h2>{{ $title }} election coverage</h2><h3>Assembly archive</h3><a href="{{ route('elections.assembly-sources',['state'=>$title]) }}">Browse {{ $title }} Assembly sources</a><p><a href="{{ route('elections.assembly') }}">Assembly results</a> · <a href="{{ route('elections.history') }}">Lok Sabha results</a></p></section><footer>Pollmedia · Official sources, historical context and visible coverage.</footer>
</main></div><script>const menu=document.querySelector(".dashboard-sidebar details"); const wide=matchMedia("(min-width:1100px)"); menu.open=wide.matches; wide.addEventListener("change",event=>{menu.open=event.matches;});</script></body></html>
