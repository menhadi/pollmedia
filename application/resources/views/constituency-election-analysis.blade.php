@php $analysis=app(\App\Services\HistoricalElectionAnalytics::class)->summarize([$selected]); @endphp
<section class="card" id="constituency-analysis"><h2>The vote at a glance</h2><div class="metrics"><div class="metric"><span>Votes polled</span><strong>{{ $analysis['polled']===null?'—':number_format($analysis['polled']) }}</strong></div><div class="metric"><span>Turnout</span><strong>{{ $analysis['turnout']===null?'—':number_format($analysis['turnout'],2).'%' }}</strong></div><div class="metric"><span>Winning margin · votes</span><strong>{{ $analysis['margin']===null?'—':number_format($analysis['margin']) }}</strong></div><div class="metric"><span>Margin · percentage points</span><strong>{{ $analysis['margin_percent']===null?'—':number_format($analysis['margin_percent'],2) }}</strong></div></div>
@if($analysis['parties'])<h3>Party vote shares</h3><p class="small">Share of recorded candidate votes plus NOTA. Bars share a 0–100% scale. The candidate table below retains the original figures.</p><div class="trend-chart">@foreach($analysis['parties'] as $item)<div class="trend-row"><span>{{ $item['party'] }}</span><span class="trend-track" aria-hidden="true"><span class="trend-fill" style="width:{{ $item['share'] }}%"></span></span><span class="trend-value">{{ number_format($item['share'],2) }}%</span></div>@endforeach</div>@else<p class="small">Summary figures are unavailable where the source needs review, candidate votes are incomplete or the contest has multiple seats. See the original data and notes below.</p>@endif</section>
