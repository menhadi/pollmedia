@extends('geography-layout')
@section('title', 'Historical by-election results')
@section('content')
<h1>Historical by-election results</h1>
<p>{{ $all->count() }} source-linked constituency records. <a href="{{ route('elections.by-elections') }}">Original source tables and archive coverage</a>.</p>
<p class="notice">Historical names and state labels are retained as reported. Editions can overlap. The older winner-and-runner summaries do not establish complete candidate coverage, and source discrepancies remain marked †.</p>
<form method="get" class="card filters"><div><label for="year">Election year</label><select id="year" name="year">@foreach($years as $option)<option value="{{ $option }}" @selected($year === $option)>{{ $option }}</option>@endforeach</select></div><div><label for="state">State label in source</label><select id="state" name="state"><option value="">All recorded states</option>@foreach($states as $state)<option value="{{ $state }}" @selected(($input['state'] ?? '') === $state)>{{ $state }}</option>@endforeach</select></div><div><label for="kind">Constituency type</label><select id="kind" name="kind"><option value="">PC and AC</option><option value="pc" @selected(($input['kind'] ?? '') === 'pc')>Parliamentary (PC)</option><option value="ac" @selected(($input['kind'] ?? '') === 'ac')>Assembly (AC)</option></select></div><button>Find results</button></form>
<form method="get" class="card filters"><input type="hidden" name="year" value="{{ $year }}">@foreach(['state', 'kind'] as $filter)@if(isset($input[$filter]))<input type="hidden" name="{{ $filter }}" value="{{ $input[$filter] }}">@endif @endforeach<div><label for="record">Constituency and report period</label><select id="record" name="record">@foreach($choices as $choice)<option value="{{ $choice['id'] }}" @selected(($record['id'] ?? '') === $choice['id'])>{{ $choice['state'] ?? 'State not recorded' }} · {{ $choice['constituency'] ?? 'Name requires review' }} · {{ $choice['period'] }}</option>@endforeach</select></div><button @disabled($choices->isEmpty())>Open result</button></form>
@if($record)
<section class="card"><h2>{{ $record['constituency'] ?? 'Constituency name requires review' }} @if($record['notes'])<a href="#notes"><sup>†</sup></a>@endif</h2><p>{{ $record['state'] ?? 'State requires review' }} · {{ $record['year'] }} · {{ strtoupper($record['kind'] ?? '') }}</p>
<p><a href="{{ $record['source_url'] }}" target="_blank" rel="noopener noreferrer">Official source</a> · <a href="{{ route('elections.by-elections', ['edition' => $record['edition'], 'source' => $record['raw_table_file']]) }}">All original cells and definitions</a></p>
<div class="scroll"><table><thead><tr><th>Candidate / reported name</th><th>Party as reported</th><th>Votes</th><th>Source row</th></tr></thead><tbody>@foreach($record['candidates'] as $candidate)<tr><td>{{ $candidate['name'] }}@if(($candidate['role'] ?? '') === 'reported_winner') <span class="muted">(reported winner)</span>@endif</td><td>{{ $candidate['party'] ?: 'Not reported' }}</td><td>{{ $candidate['votes'] === null ? 'Not available †' : number_format($candidate['votes']) }}</td><td>{{ $candidate['source_row'] }}</td></tr>@endforeach</tbody></table></div>
@if($record['notes'])<section id="notes"><h3>† Source and extraction notes</h3><ul>@foreach($record['notes'] as $note)<li>{{ $note }}</li>@endforeach</ul></section>@endif
@if(!empty($record['reason']))<p>Reason for by-election as reported: {{ $record['reason'] }}</p>@endif
</section>
@else<p class="notice">No prepared results match this selection. This does not mean that no election took place.</p>@endif
@endsection
